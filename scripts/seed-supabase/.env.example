# ─────────────────────────────────────────────────
# Humi HRM – Supabase Seeder Config
# Copy file này thành .env rồi điền thông tin vào
# ─────────────────────────────────────────────────

# Lấy từ: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://vbxyzblhcdiuaijshkls.supabase.co

# SERVICE ROLE KEY (không phải anon key!) — có quyền bypass RLS
# Lấy từ: Dashboard → Project Settings → API → service_role
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.REPLACE_ME

# Số dòng mỗi batch insert (mặc định 50)
BATCH_SIZE=50
