{
  "name": "humi-seed-supabase",
  "version": "1.0.0",
  "description": "Reset & seed Supabase database cho Humi HRM",
  "main": "seed.js",
  "scripts": {
    "seed":       "node seed.js",
    "reset":      "node seed.js --reset-only",
    "seed:fresh": "node seed.js --force"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5"
  },
  "engines": { "node": ">=18.0.0" }
}
